// Generated by arc_example — a tiny bundler built on the Arc JavaScript parser.
(() => {
"use strict";
const __modules = {};
const __cache = {};
const __require = (id) => {
  const cached = __cache[id];
  if (cached) return cached.exports;
  const module = (__cache[id] = { exports: {} });
  __modules[id](module.exports, __require);
  return module.exports;
};
// `export * from` semantics: copy everything except `default`.
const __reexport = (exports, m) => {
  for (const key of Object.keys(m)) {
    if (key !== "default") exports[key] = m[key];
  }
};

__modules["app/lib/strings.mjs"] = (exports, require) => {
function capitalize(s) {
  return s.slice(0, 1).toUpperCase() + s.slice(1);
}

exports.capitalize = capitalize;
};
__modules["app/components/greeting.mjs"] = (exports, require) => {
const { capitalize } = require("app/lib/strings.mjs");

function greet(name) {
  return `hello, ${capitalize(name)}!`;
}

exports.greet = greet;
};
__modules["app/lib/math.mjs"] = (exports, require) => {
const PI = 3.141592653589793;

function add(a, b) {
  return a + b;
}

exports.PI = PI;
exports.add = add;
};
__modules["app/lib/index.mjs"] = (exports, require) => {
// Directory entry point: `import * as lib from "./lib"` lands here.
// Exercises `export * from` and named re-exports.

__reexport(exports, require("app/lib/math.mjs"));
exports.capitalize = require("app/lib/strings.mjs").capitalize;
};
__modules["app/node_modules/left-pad/lib/leftpad.js"] = (exports, require) => {
// A very legally-distinct left-pad, resolved via package.json "main".
exports.default = function leftPad(str, len, ch = " ") {
  str = String(str);
  while (str.length < len) {
    str = ch + str;
  }
  return str;
}

};
__modules["app/main.mjs"] = (exports, require) => {
const { greet } = require("app/components/greeting.mjs");
const lib = require("app/lib/index.mjs");
const leftPad = require("app/node_modules/left-pad/lib/leftpad.js").default;
// Entry module. Exercises: extensionless relative imports, directory-index
// imports, namespace imports, bare specifiers from node_modules.

const lines = [
  greet("hayleigh"),
  `pi is roughly ${lib.PI.toFixed(2)}`,
  `2 + 3 = ${lib.add(2, 3)}`,
  leftPad("bundled by arc", 24, "·"),
];

const text = lines.join("\n");

if (typeof document !== "undefined") {
  document.getElementById("app").textContent = text;
} else {
  console.log(text);
}

};
__require("app/main.mjs");
})();
